const menu=document.querySelector('.menu-toggle');
menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));document.querySelector('#navigation').classList.toggle('open',open)});
document.addEventListener('keydown',e=>{if(e.key==='Escape'&&menu&&!document.querySelector('.nav-dropdown[aria-expanded="true"]')){const wasOpen=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded','false');document.querySelector('#navigation').classList.remove('open');if(wasOpen)menu.focus()}});
const dropdowns=[...document.querySelectorAll('.nav-dropdown')];
function closeDropdowns(except){dropdowns.forEach(button=>{if(button!==except){button.setAttribute('aria-expanded','false');document.getElementById(button.getAttribute('aria-controls')).hidden=true}})}
dropdowns.forEach(button=>button.addEventListener('click',()=>{const open=button.getAttribute('aria-expanded')!=='true';closeDropdowns(button);button.setAttribute('aria-expanded',String(open));document.getElementById(button.getAttribute('aria-controls')).hidden=!open}));
document.addEventListener('click',event=>{if(!event.target.closest('.nav-group'))closeDropdowns()});
document.addEventListener('keydown',event=>{if(event.key==='Escape'){const open=dropdowns.find(b=>b.getAttribute('aria-expanded')==='true');closeDropdowns();if(open)open.focus()}});
document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));let count=0;document.querySelectorAll('[data-category]').forEach(card=>{card.hidden=button.dataset.filter!=='all'&&card.dataset.category!==button.dataset.filter;if(!card.hidden)count++});document.querySelector('#filter-count').textContent=`${count} packaging example${count===1?'':'s'}`}));
const fold={shipping:['Ship less empty space.','Flat-pack delivery can reduce the volume occupied by empty boxes. Compare packed master-carton dimensions and units per carton for your actual specification.'],assembly:['Assemble when you need it.','Confirm the assembly method with a physical sample. Plan the opening, corner fixing and insert installation around the team packing your products.'],presentation:['Keep the premium reveal.','Coordinate the exterior paper, closure, artwork and interior fit. The final sample should confirm both the flat-pack structure and the finished gift presentation.']};
document.querySelectorAll('[data-fold]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-fold]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));document.querySelector('#fold-title').textContent=fold[button.dataset.fold][0];document.querySelector('#fold-copy').textContent=fold[button.dataset.fold][1]}));
const form=document.querySelector('#rfq');
if(form){
 const p=new URLSearchParams(location.search).get('product');if(p)form.elements.product.value=p.replaceAll('-',' ').slice(0,300);
 function prepare(){if(!form.reportValidity())return null;const d=new FormData(form);const brief=`Hello Crown Win,\n\nI would like to discuss custom packaging.\n\nName: ${d.get('name')}\nWork email: ${d.get('email')}\nCompany: ${d.get('company')}\nDelivery country: ${d.get('country')}\nPackaging: ${d.get('product')}\nQuantity: ${d.get('quantity')}\nProduct dimensions: ${d.get('dimensions')}\nMatching paper bags: ${d.has('bags')?'Yes':'Not requested'}\nAssess foldable structure: ${d.has('foldable')?'Yes':'Not requested'}\n\nProject details:\n${d.get('details')}\n\nPlease advise suitable options and the next steps.`;document.querySelector('#draft').hidden=false;document.querySelector('#draft-text').value=brief;document.querySelector('#email-draft').href='mailto:kevinlu@box-label.com?subject='+encodeURIComponent('Packaging enquiry: '+d.get('product'))+'&body='+encodeURIComponent(brief);document.querySelector('#form-status').textContent='Draft prepared. Nothing has been sent.';return brief}
 form.addEventListener('submit',e=>{e.preventDefault();prepare()});
 document.querySelector('#copy-brief').addEventListener('click',async()=>{const brief=prepare();if(!brief)return;try{await navigator.clipboard.writeText(brief);document.querySelector('#form-status').textContent='Brief copied. Paste it into an email to kevinlu@box-label.com.'}catch{const text=document.querySelector('#draft-text');text.focus();text.select();document.querySelector('#form-status').textContent='Your brief is selected below. Copy it and paste it into your email.'}});
}
